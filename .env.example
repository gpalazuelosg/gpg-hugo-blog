# Copy to .env.local for local dev. Values live in Vercel env vars (PRD §3.3).
SANITY_PROJECT_ID=
SANITY_DATASET=production
SANITY_API_READ_TOKEN=
# Build fails if the Sanity fetch returns fewer posts than this.
# 0 until first publish (Day 5), then 1 in production.
MIN_POSTS=0
